import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Keep server runtime enabled for API routes on Cloudflare Pages.
  // Do NOT use `output: "export"`.
};

export default nextConfig;
